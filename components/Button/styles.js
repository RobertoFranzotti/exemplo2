import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  button: {
    width: '100%',
    backgroundColor: '#ed3',
    padding: 5,
    borderWidth: 1,
    borderStyle: "dashed",
    borderColor: "#000",
    borderRadius: 4,

    marginTop: 10,
  },

  text: {
    fontSize: 16,
    color: "#000",
    textAlign: "center",
  },
});